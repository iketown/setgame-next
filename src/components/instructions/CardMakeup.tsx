import React from "react";
import { Typography } from "@material-ui/core";

const CardMakeup = () => {
  return (
    <div>
      step 1. this is how a card is made up.
      <Typography>there are shapes, colors </Typography>
    </div>
  );
};

export default CardMakeup;
